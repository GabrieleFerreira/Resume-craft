type EditorProps = {
  value: string;
  onChange?: (value: string) => void;
  className?: string;
};

export const Editor = ({ value, className, onChange }: EditorProps) => {
  return <div></div>;
};
